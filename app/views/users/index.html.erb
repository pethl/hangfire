<h2>Users</h2>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Lastname</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Addresses</th>

     
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.first_name %></td>
        <td><%= user.lastname %></td>
        <td><%= user.email %></td>
        <td><%= user.phone %></td>
       
        <td><strong>Billing: </strong><%= user.bill_addr1 %>
        <%= user.bill_addr2 %>
        <%= user.bill_addr3 %>
        <%= user.bill_addr4 %>
        <%= user.bill_postcode %><br>
        
        <strong>Delivery: </strong><%= user.delivery_addr1 %>
        <%= user.delivery_addr2 %>
        <%= user.delivery_addr3 %>
        <%= user.delivery_addr4 %>
        <%= user.delivery_postcode %></td>
  
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>
