<p id="notice"><%= notice %></p>


<h3>
  <strong><%= @product.name %></strong></h3>
  <%= @product.desc %>
  

<% if @product.productitems.any? %>

  <table class="table  table-striped">
    <thead>
      <tr>
        <th>Ingredient </th>
        <th>Volume </th>
        <th>Price per (p/g)</th>
        <th>Total cost</th>
      </tr>
    </thead>
    <tbody>
       <% @product.productitems.each do |item| %>
        <tr>
          <td><%= Ingredient.where(:id => item.ingredient_id).first.name %></td>
          <td><%= item.volume %></td>
          <td><%= Productitem.average_price(item.ingredient_id) %></td>
          <td><%= Productitem.total_price(item) %></td>
          </tr>      
      <% end %>
    </tbody>
     <tr>
     <td colspan="3"></td>
     <td>    
     <%= Product.get_total_price(@product.id) %>
        (Total Price) <br>
        
       </td>
      <tr>
  </table>  
  
  <% else %>
  
  There are no ingredients associated with this product.<br>
  To add an ingredient go to edit.
  
    <% end %>
    <br><br>



<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'All HF products', products_path %>
